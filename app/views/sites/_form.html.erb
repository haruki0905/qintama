<%= form_with(model: site, local: true) do |form| %>
  <% if site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
        <% site.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :color_code %>
    <%= form.text_field :color_code %>
  </div>

  <div class="field">
    <%= form.label :public_uid %>
    <%= form.text_field :public_uid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
